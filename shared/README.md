# shared

- `styles/` — the EnterpriseSG-brand CSS library (`tokens.css`, `base.css`, `components.css`). See `styles/README.md` for the token list and the decisions behind it.
- Types and utilities used by both `frontend` and `backend` go here too, once there are any.
